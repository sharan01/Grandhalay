#ifndef ADDCOPYWIZARD_H
#define ADDCOPYWIZARD_H

#include <QWizard>

class AddCopyWizard : public QWizard
{
    Q_OBJECT
public:
    explicit AddCopyWizard(QWidget *parent = 0);
    
signals:
    
public slots:
    
};

#endif // ADDCOPYWIZARD_H
